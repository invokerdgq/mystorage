<block>
    <button class="{{'float-menu__item ' + (hide ? 'hidden' : '')}}" bindtap="funPrivateIiiZf" open-type="{{openType}}">
        <view class="{{iconPrefixClass + ' ' + iconPrefixClass + '-' + icon}}"></view>
        <slot></slot>
    </button>
</block>